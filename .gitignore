# Generated / runtime artifacts
lab/compose/generated/
lab/results/
lab/.venv/
lab/bin/

# Vendored third-party dependency tree of the nvb/epaxos library (133 MB).
# The library predates Go modules and ships its dependencies through dep
# (Gopkg.lock). The pinned upstream commit plus Gopkg.lock fully determine
# this tree; see lab/upstream/nvb-epaxos/UPSTREAM.md for how to restore it.
lab/upstream/nvb-epaxos/vendor/

# LaTeX build artifacts
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.pdf
paper/main.fdb_latexmk
paper/main.fls

# Python
__pycache__/
*.pyc

# OS
.DS_Store