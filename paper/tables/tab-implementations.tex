\begin{table}[t]
\caption{The four evaluated implementations. Each protocol is represented by
two independent implementations; the lab adapters add only orchestration,
monitoring, and the communication-delay injection described in
Section~\ref{sec:setup}, never consensus logic.}
\label{tab:implementations}
\begin{tabular}{@{}lp{2.6cm}p{2.6cm}p{2.6cm}@{}}
\toprule
Implementation & Consensus core & Storage & Delay injection \\
\midrule
Raft (HashiCorp) & \texttt{hashicorp/raft} v1.7.3 & BoltDB (persistent) & transport wrapper \\
Raft (etcd) & \texttt{go.etcd.io/raft} v3.7.0 & WAL (persistent, batched fsync) & per-peer sender sleep \\
EPaxos (original) & \texttt{efficient/epaxos} @ \texttt{791b115} & in-memory & send-path sleep \\
EPaxos (nvb) & \texttt{nvanbenschoten/epaxos} @ \texttt{425bd36} & in-memory & per-peer sender sleep \\
\bottomrule
\end{tabular}
\end{table}